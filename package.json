{
  "name": "t4business",
  "version": "1.0.0",
  "main": "server.js",
  "description": "T4G for Business - Flutter Web Application",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server.js",
    "build:web": "flutter build web --no-tree-shake-icons",
    "heroku-postbuild": "echo 'Skipping Flutter build on Heroku. Ensure build/web is committed.'",
    "deploy": "npm run build && firebase deploy --only hosting",
    "deploy:message": "npm run build && firebase deploy --only hosting -m",
    "quick-deploy": "./quick-deploy.sh",
    "full-deploy": "./deploy.sh",
    "serve": "firebase serve --only hosting",
    "clean": "flutter clean && flutter pub get"
  },
  "dependencies": {
    "firebase": "^11.8.1",
    "express": "^4.18.2"
  },
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/trash4goods/t4business-frontend.git"
  },
  "author": "T4G Team",
  "license": "MIT"
}
